!yamlscript/v0

defn commands(number):
  cmds =:: [wink, double blink, close your eyes, jump]

  ->> range(cmds.#):
    map: \(number.bit-test(_) && cmds.nth(_))
    filter: a
    call:
      fn(commands):
        if number.bit-test(cmds.#):
          commands.@<
          commands
