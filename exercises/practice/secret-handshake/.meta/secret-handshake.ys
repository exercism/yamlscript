!yamlscript/v0

cmds =:: [wink, double blink, close your eyes, jump]

L =: count  # XXX

defn commands(number):
  commands =:
    filter identity:
      map _ range(L(cmds)):
        fn(n): number.bit-test(n) && cmds.$n

  if number.bit-test(L(cmds)):
    reverse(commands)
    commands
