!yamlscript/v0

defn isogram(string):
  string.#.0? ||:
    lc(string).$@      \"Split lc string into chars"
      .filter(/^\w$/)  \"Keep letters only"
      .distinct?(*)    \"Check if distinct letters"
