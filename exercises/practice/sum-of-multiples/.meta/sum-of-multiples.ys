!yamlscript/v0

defn sum(factors limit):
  if empty?(factors):
    then: 0
    else:
      apply +:
        reduce _ [] factors:
          fn(nums num):
            distinct:
              concat nums:
                when t?(num):
                  range(num limit num)
