!yamlscript/v0

defn distance(strand1 strand2):
  when (strand1.count() != strand2.count()):
    die:: strands must be of equal length

  count:
    filter true?:
      map \(%1 != %2): strand1 strand2
