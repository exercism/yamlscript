!yamlscript/v0

defn valid(value):
  digits =: value.replace(' ')

  and:
    digits.# >=: 2
    digits !~: /[^0-9]/
    digits.$@:
      .map(to-num).@<
      .conj(0).partition(2)
      .map(munge).@_.@+.mod(10).0?

defn munge([a b]):
  b *=: 2
  vector a:
    if b > 9: (b - 9) b
