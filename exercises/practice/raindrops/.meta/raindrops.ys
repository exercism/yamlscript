!yamlscript/v0

dict =:: { 3: Pling, 5: Plang, 7: Plong }

defn convert(number):
  words =:
    reduce _ [] [3 5 7]:
      fn(acc num):
        concat acc:
          if zero?(number % num):
            +[dict.$num]
            []

  if empty?(words):
    str: number
    str: words*
