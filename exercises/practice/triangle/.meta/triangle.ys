!yamlscript/v0

defn equilateral(sides):
  and eq(sides*): sides.0 > 0

defn isosceles(sides):
  -[a b c] =: sort(sides)
  and ((a + b ) > c):
    sides.frequencies().# <= 2

defn scalene(sides):
  boolean:
    when-not isosceles(sides):
      -[a b c] =: sort(sides)
      gt: (a + b) c
