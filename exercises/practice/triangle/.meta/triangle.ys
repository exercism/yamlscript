!yamlscript/v0

defn equilateral(sides):
  (sides.0 > 0) && eq(sides*)

defn isosceles(sides):
  +[a b c] =: sort(sides)
  and:
    ((a + b ) > c)
    (N(sides.frequencies()) <= 2)

defn scalene(sides):
  and:
    not: isosceles(sides)
    do:
      +[a b c] =: sort(sides)
      gt: (a + b) c
