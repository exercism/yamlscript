!yamlscript/v0

defn square-of-sum(number):
  (1 .. number).sum(*) ** 2

defn sum-of-squares(number):
  (1 .. number).map(squared).sum(*)

defn difference-of-squares(number):
  square-of-sum(number) -
  sum-of-squares(number)
