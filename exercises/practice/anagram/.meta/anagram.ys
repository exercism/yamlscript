!yamlscript/v0

defn find-anagrams(subject candidates):
  filter \(anagram %1 subject): candidates

defn anagram(word1, word2):
  word1 =: word1.lower-case()
  word2 =: word2.lower-case()
  and:
    (word1 != word2)
    (word1.sort() = word2.sort())
