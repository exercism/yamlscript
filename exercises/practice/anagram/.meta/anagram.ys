!yamlscript/v0

defn find-anagrams(subject candidates):
  filter _ candidates:
    \(anagram %1 subject)

defn- anagram(word1 word2):
  # XXX use .=
  word1 =: word1.lc()
  word2 =: word2.lc()
  and:
    ne: word1 word2
    eq: sort(word1)
        sort(word2)
