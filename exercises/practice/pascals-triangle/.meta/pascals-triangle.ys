!yamlscript/v0

defn rows(count):
  # Implement the 'rows' function.

# (ns pascals-triangle)

# (defn- next-row [row]
#   (lazy-seq (->> (partition-all 2 1 row)
#                  (map (partial apply +'))
#                  (cons 1))))

# (def triangle (iterate next-row [1]))
