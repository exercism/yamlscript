!yamlscript/v0

defn rows(count):
  reduce _ [] range(1 count.++):
    fn(tri row):
      conj tri:
        or _ [1]:
          when row > 1:
            mapv _ (-1 .. row.sub(2)):
              \(last(tri).get(_ 0):N +
                last(tri).get(_.++).or(0):N)
